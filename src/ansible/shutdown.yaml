---
- name: shutdown
  hosts: "{{ target_hosts }}"
  become: true
  become_user: root
  tasks:
   - name: shutdown
     community.general.shutdown:

   - name: print message
     ansible.builtin.debug:
       msg: shutdown
